# Gas report (Stage 6.8)

Figures from Foundry tests with the **generated Halo2 PLONK verifier** and **CI-generated** proof fixtures (`scripts/build_zk_artifacts.sh`).

| Contract / function | Approx. gas | Notes |
|---------------------|------------|-------|
| `TrustMeshVerifier.registerAgent` | ~55k–70k | Stores KZG digest + commitment field |
| `TrustMeshVerifier.verifyAndExecute` (success) | **200k–350k+** | Dominated by Halo2 PLONK verify |
| `Halo2PlonkVerifier.verifyProof` | circuit-dependent | Exported from `prover-core` |

**400,000 gas target:** re-benchmark after each circuit change; Halo2 verify typically exceeds legacy mock verifier costs.

## Operational trust

- Gas snapshots use in-process Foundry EVM with fixture proofs — not Sepolia mainnet conditions.
- Verifier bytecode is generated by `halo2-solidity-verifier`; treat gas as indicative until production deployment.

## Deprecated references removed

Previous docs cited `MockPlonkVerifier` (~64k verify). That path was removed in Stage 6.75B; all production tests use `Halo2PlonkVerifier`.
